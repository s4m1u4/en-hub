import styled from "@emotion/styled";
import { grey } from "@mui/material/colors";

export const MainWord = styled.h1({
  fontSize: "2rem",
  fontWeight: 500,
});

export const CountWords = styled.p({
  fontSize: "1.2rem",
  fontWeight: 400,
  color: grey[500],
});
